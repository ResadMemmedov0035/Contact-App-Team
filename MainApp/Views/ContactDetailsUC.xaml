<UserControl x:Class="MainApp.Views.ContactDetailsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainApp.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="570" d:DesignWidth="875"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             Foreground="Wheat"
             Background="White"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}">
           
             <!--Background="{DynamicResource MaterialDesignDarkBackground}"-->
   
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ListBox  Background="{StaticResource MaterialDesignToolBarBackground}">
            <ListBoxItem Background="{StaticResource SecondaryHueMidBrush}" Width="auto">
                
                <StackPanel Orientation="Horizontal">
                    
                    <materialDesign:PackIcon Kind="AccountGroup" Height="100" Width="100"/>
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Margin="35,0,0,70">
                        <Run FontSize="35">255</Run>
                    </TextBlock>
                </StackPanel>
                
            </ListBoxItem>

            <ListBoxItem   Background="{StaticResource PrimaryHueMidBrush}" Width="auto">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AccountHeart" Height="100" Width="100"/>
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Margin="35,0,0,70">
                        <Run FontSize="35">255</Run>
                    </TextBlock>
                </StackPanel>

            </ListBoxItem>
            <ListBoxItem  Background="{StaticResource PrimaryHueMidBrush}" Width="auto">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AccountMultiplePlus" Height="100" Width="100"/>
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Margin="35,0,0,70">
                            <Run FontSize="35">255</Run>
                    </TextBlock>
                </StackPanel>
            </ListBoxItem>

            <ListBoxItem  Background="{StaticResource PrimaryHueMidBrush}" Width="auto">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AccountOff" Height="100" Width="100"/>
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Margin="35,0,0,70">
                            <Run FontSize="35">255</Run>
                    </TextBlock>
                </StackPanel>
            </ListBoxItem>
        </ListBox>

    </Grid>
</UserControl>

         