<UserControl x:Class="MainApp.Views.AddContactUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainApp.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             Foreground="Wheat"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignDarkBackground}"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock Margin="5"  HorizontalAlignment="Center">Add First Name</TextBlock>
            <TextBox  VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <TextBlock HorizontalAlignment="Center">Add Last Name</TextBlock>
            <TextBox/>

            <TextBlock HorizontalAlignment="Center">Add Phone Number</TextBlock>
            <TextBox/>

            <TextBlock HorizontalAlignment="Center">Add Email Address</TextBlock>
            <TextBox/>

            <TextBlock HorizontalAlignment="Center">Add Photo Url</TextBlock>
           <TextBox/>

            <TextBlock HorizontalAlignment="Center">Add Job</TextBlock>
           <TextBox/>
        </StackPanel>
    </Grid>
</UserControl>

             <!--TextElement.Foreground="{DynamicResource AttentionToActionBrush}"-->
        